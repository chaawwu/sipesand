import React from 'react';
import ReactDOM from 'react-dom/client';
import MitraPortalApp from './MitraPortalApp.jsx';
import '../../index.css';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <MitraPortalApp />
  </React.StrictMode>,
);
